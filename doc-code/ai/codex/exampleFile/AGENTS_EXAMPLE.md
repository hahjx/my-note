## 用户输入格式约定

- 用户输入中被反引号包裹的内容，优先视为精确定位信息。
- `路由`、`页面路径`、`按钮文案`、`弹窗标题`、`表格列名`、`Tab 名`、`接口名`、`字段名` 都可能使用反引号标记。
- 对反引号内容优先原样搜索，不先翻译、改写或猜测同义词。
- 如果当前环境存在 `$frontend-requirement-parser`，解析需求时优先使用该 Skill 的规则。
